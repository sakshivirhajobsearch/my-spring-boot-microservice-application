server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security_service_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: mysecretkey
  expiration: 3600000
